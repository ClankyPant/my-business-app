import React from 'react';

interface Props {
  // Add props here! 
}

const page : React.FC<Props> = ({}) => {
    return (
      <>
        Alguma coisa sobre alguma coisa
      </>
    );
};

export default page;